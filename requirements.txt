# ═══════════════════════════════════════════════════════
#  JARVIS ULTIMATE 2026 — зависимости
#  Установка: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════

# ── Telegram ─────────────────────────────────────────
telethon>=1.36.0

# ── LLM (Groq / llama-4-scout) ──────────────────────
groq>=0.9.0

# ── HTTP клиент (погода, поиск, Яндекс.Диск) ────────
httpx>=0.27.0

# ── Поиск в интернете (DuckDuckGo) ──────────────────
duckduckgo-search>=6.0.0

# ── PDF ──────────────────────────────────────────────
PyPDF2>=3.0.0

# ── Word-документы (.docx) ───────────────────────────
python-docx>=1.1.0

# ── Логирование ──────────────────────────────────────
loguru>=0.7.0

# ── .env файл ────────────────────────────────────────
python-dotenv>=1.0.0

# ── Playwright (для JS-сайтов, опционально) ───────────
# После установки выполнить: playwright install chromium
playwright>=1.40.0

# ── RAG / векторный поиск (опционально, тяжёлые) ─────
# Раскомментируйте только если нужен RAG поиск по файлам
# langchain>=0.2.0
# langchain-community>=0.2.0
# langchain-huggingface>=0.0.3
# langchain-chroma>=0.1.0
# sentence-transformers>=2.7.0
# chromadb>=0.5.0
