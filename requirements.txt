# ═══════════════════════════════════════════════════════
#  JARVIS ULTIMATE 2026 — зависимости
#  Установка: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════

telethon>=1.36.0
groq>=0.9.0
httpx>=0.27.0
duckduckgo-search>=6.0.0
PyPDF2>=3.0.0
python-docx>=1.1.0
loguru>=0.7.0
python-dotenv>=1.0.0
google-auth>=2.0.0
cryptography>=42.0.0
rsa>=4.9

# Playwright (опционально — для JS сайтов)
# После установки: playwright install chromium
# playwright>=1.40.0

# GigaChat SDK (опционально — бот использует прямые HTTP запросы)
# gigachat>=0.1.0
