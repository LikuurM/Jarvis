# ═══════════════════════════════════════════════════
# JARVIS ULTIMATE 2026 — зависимости
# Установка: pip install -r requirements.txt
# ═══════════════════════════════════════════════════

# Telegram
telethon>=1.36.0

# LLM
litellm>=1.48.0

# RAG / База знаний (CPU-only, без CUDA)
langchain-core>=0.3.0
langchain-chroma>=0.1.4
langchain-community>=0.3.0
langchain-huggingface>=0.1.0
# sentence-transformers CPU only — без torch+cuda
sentence-transformers>=2.2.0,<3.0.0
chromadb>=0.5.0

# PyTorch CPU (без GPU — избегаем скачивания CUDA ~2GB)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu

# Поиск
ddgs>=0.1.0

# Браузер (опционально — для deep search)
playwright>=1.44.0

# Документы
PyPDF2>=3.0.0
python-docx>=1.1.0

# Логи
loguru>=0.7.0

# .env
python-dotenv>=1.0.0

# HTTP
httpx>=0.27.0

# Git (для push на GitHub из бота)
gitpython>=3.1.40
